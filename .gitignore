# Secrets
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/

# Generated mockup packs
outputs/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Claude Code local settings (per-machine)
.claude/settings.local.json

# Streamlit local secrets (production secrets live in Streamlit Cloud UI)
.streamlit/secrets.toml

# Internal brand-strategy docs — kept on Pranav's laptop only, never pushed.
# Claude Code still reads CLAUDE.md from the working tree on session start,
# but it's not part of the deployed app.
CLAUDE.md
SPEC.md
